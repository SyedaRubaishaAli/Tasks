// Task 01

studentData = ["Rubaisha","BCP027","Blockchain",true,500]

function ShowData() {
    console.log(studentData);
}
ShowData()

// Push:to insert Something in the Array at the end

studentData.push("Syeda")
ShowData()

// Pop:to remove something in the Array at the end

studentData.pop("Syeda")
ShowData()

// Unshift:to insert something in the Array at first

studentData.unshift("Usman")
ShowData()

// Shift: to remove something in the Array at first

studentData.shift("Usman")
ShowData()